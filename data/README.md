# Data

Large data files are not committed.

Expected primary raw dataset path:

```text
data/raw/260520_putative_nucleosomal.parquet
```

Use `data/sample/` only for tiny committed fixtures and smoke tests.
